#include <iostream>

using namespace std;

int main{

    // Take input city, call weather.cpp functions and output results
    string city_name;

    cout << "Enter a city: ";
    std::getline(std::cin, city_name);

    return 0;
}